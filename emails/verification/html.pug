doctype html
html(lang="pt-BR")
head
  meta(charset="UTF-8")
  meta(name="viewport" content="width=device-width, initial-scale=1.0")
  title Confirmação de E-mail - #{appName}
  style(type="text/css").
    /* Reset e estilos base */
    body, html {
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
      line-height: 1.6;
      color: #333333;
      background-color: #f7f7f7;
    }
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
    }
    .header {
      padding: 30px 20px;
      text-align: center;
      background-color: #394634;
    }
    .logo {
      max-height: 60px;
    }
    .content {
      padding: 30px;
    }
    .footer {
      padding: 20px;
      text-align: center;
      font-size: 12px;
      color: #777777;
      background-color: #f5f5f5;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #394634;
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 4px;
      font-weight: bold;
      margin: 20px 0;
    }
    .divider {
      height: 1px;
      background-color: #eaeaea;
      margin: 25px 0;
    }
    @media only screen and (max-width: 600px) {
      .content {
        padding: 20px;
      }
      .button {
        display: block;
        margin: 20px auto;
        text-align: center;
      }
    }
body
  table.email-container(width="100%" cellpadding="0" cellspacing="0" border="0")
    tr
      td.header
        img.logo(src=`${appUrl}/images/logo-email.png` alt=appName width="180")
    tr
      td.content
        h2(style="margin-top: 0;") Olá,
        p Obrigado por se cadastrar no #{appName}! Estamos felizes em tê-lo conosco.
        
        p Para ativar sua conta e começar a usar nossos serviços, por favor confirme seu endereço de e-mail:

        div(style="text-align: center; margin: 30px 0;")
          a.button(href=verificationUrl) Confirmar E-mail

        p(style="font-size: 14px; color: #666666; text-align: center;") Ou copie e cole este link em seu navegador:
        p(style="word-break: break-all; font-size: 14px; color: #666666; background: #f9f9f9; padding: 10px; border-radius: 4px;") #{verificationUrl}

        .divider

        p(style="margin-bottom: 0;") Se você não se cadastrou no #{appName}, por favor ignore este e-mail.
        
        p(style="margin-top: 25px;") Atenciosamente,
        p(style="font-weight: bold; margin-bottom: 0;") Equipe #{appName}
    tr
      td.divider
    tr
      td.footer
        | © #{year} #{appName}. Todos os direitos reservados.
        br
        | Caso não tenha solicitado este e-mail, por favor ignore.